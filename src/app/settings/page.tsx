/**
 * Settings Page Gateway
 * Minimal gateway component following FSR pattern
 */

import { SettingsPage } from '@/components/settings';

export default function Settings() {
  return <SettingsPage />;
}