import CreateNoteForm from '@/components/create/components/CreateNoteForm';

const CreatePage = () => {
  return (
    <main className="min-h-screen bg-background text-foreground">
      <CreateNoteForm />
    </main>
  );
};

export default CreatePage;
